from multiprocessing.connection import answer_challenge
import os
from pickle import TRUE
import sys
from this import d
from joblib import PrintTime
import numpy as np
from tkinter.messagebox import YES
from typing import List
import time
import cv2
name=('请输入即将过生日的小猪的名字')
print (name)
name=input()
if name == (''):
    print('你输入的名为空,别开玩笑了ya')
    head=[('请按q退出')%d]
    print(head)
    head=[]
    head=input('')
    tail = ['q']
if len(head) == len(tail):
    print('别想啦，退不出去的.快陪我')
if name == ('郑靓如'):
    print("'name'，接下来玩个游戏请选择yes or no")
print('接下来玩个游戏，请选择yes or no')
ans = input('')
('yes')== True
if ans == ('yes'):
            print('谢谢你选择继续陪我玩，不然浪费了我写了这么久的代码捏')
            def operation(self,arg:List[List[int]]) -> List[int]:
                                                                res=[]
                                                                while matrix:
                                                                   res+=matrix.pop(0)
                                                                   matrix = List(zip*(matrix))[::-1]
                                                                return res
            matrix=[[1,2,3],[4,98,4]]
            count=eval(input())
            print (list(count))
            Doc=[matrix+list(count)]
            matrix=Doc
            Doc=np.array(Doc)
            operation(Doc[int])
print('感谢你等了这么久')
a=[520,13,14]*1000
print(a)
array_of_img = []
def read_directory(directory_name):
                               for filename in os.listdir(r"./"+directory_name):
                                   img = cv2.imread(directory_name + "/" + filename)
                                   array_of_img.append(img)
                                   print(array_of_img)
read_directory("file")

